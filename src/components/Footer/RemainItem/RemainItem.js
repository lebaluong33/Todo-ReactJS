import React, { Fragment } from 'react';

const remainItem = (props) => (
  <Fragment>
    <strong>20</strong>
    <span> </span>
    <span>items</span>
    <span> left</span>
  </Fragment>
);

export default remainItem;